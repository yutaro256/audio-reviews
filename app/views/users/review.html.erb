<div>
  <% if @user.image? %>
    <%= image_tag @user.image.url, class: 'rounded-circle' %>
  <% else %>
    <%= image_tag 'default.jpg', class: 'rounded-circle' %>
  <% end %>
  <h3><%= @user.name %></h3>
  <%= link_to '編集', edit_user_path(@user) %>
</div>

<div>
  <%= link_to '気になるリスト', @user %>
</div>

<div>
  自分の投稿
</div>

<%= render 'shared/review_index', reviews: @reviews %>